{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "npm ci || pnpm install || yarn install",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
